# claw — agent guide

`claw` is a Bun + `argc` CLI: an OKF-style markdown knowledge indexer/reader with
a per-repo file-watch daemon. Three commands — `index`, `read`, `daemon`.

## Conventions

- **Schema-first.** `src/schema.ts` is the public contract — command names,
  flags, examples, and help all live there. Change it deliberately.
- **Output is YAML by default**, `--json` for pipes. Errors are structured
  (`src/errors.ts`) with exit codes 2 (usage) / 3 (not found) / 4 (forbidden).
- **`bun run check`** (fmt:check + typecheck + test) must stay green. Use
  `oxfmt` and `tsgo`; never introduce eslint / prettier / tsc.

## Architecture

- Entry/wiring: `src/main.ts`. Command handlers: `src/handlers/*`. Everything
  else in `src/` is reusable logic.
- `wiki.ts` — `scanDocs` (enumerates via `git ls-files`, glob fallback),
  `buildIndex` (a YAML list), and the AGENTS.md pointer-block injector.
- `markdown.ts` — markdown-it backed `--toc` / `--section` / summary. Output is
  validated byte-for-byte against the `ctx` tool; keep it aligned.
- `daemon.ts` — git-root anchor, O_EXCL lock (pid + start time), heartbeat-TTL
  self-reap, `@parcel/watcher`. No snapshot. Design: `docs/index-daemon.md`.

## Invariants — don't break these silently

- `index.yaml` is a generated artifact: gitignored, never committed.
- `scanDocs` indexes only frontmatter-bearing docs; the skill install roots
  `.agents/skills/` and `.claude/skills/` are excluded (skill territory).
- The daemon must never leak a spinning process — tests SIGKILL spawned daemons
  by `--root` and assert the process actually exits.
- Tests are the contract. Changing behavior without a test usually means it
  isn't locked.

## Knowledge

<!-- claw:index -->
<!-- Generated by `claw index`. -->

The documents in this workspace are indexed in `./index.yaml` — one entry
per doc, with its path and frontmatter (type, description, `when`, …). Read
it to discover what docs exist and when to consult them.

<!-- /claw:index -->
